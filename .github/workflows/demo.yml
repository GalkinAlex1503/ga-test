name: Demo Workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
        - name: print to console
          run: echo Print test step!
        - name: print same string
          run: |
            echo string 1
            echo string 2
          
